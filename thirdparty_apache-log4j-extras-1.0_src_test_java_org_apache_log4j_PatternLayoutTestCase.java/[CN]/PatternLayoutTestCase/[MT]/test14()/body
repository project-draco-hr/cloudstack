{
  configure("input/pattern/patternLayout14.properties");
  common();
  Transformer.transform(TEMP,FILTERED,new Filter[]{new LineNumberFilter(),new SunReflectFilter(),new JunitTestRunnerFilter()});
  assertTrue(compare(FILTERED,"witness/pattern/patternLayout.14"));
}
