def getdev(path):
    if match_dm(path):
        newpath = path.replace('/dev/mapper/', '/dev/disk/by-id/scsi-')
    else:
        newpath = path
    return os.path.realpath(newpath).split('/')[(-1)]
