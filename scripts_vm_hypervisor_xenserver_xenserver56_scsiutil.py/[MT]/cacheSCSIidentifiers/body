def cacheSCSIidentifiers():
    SCSI = {}
    cmd = ['sg_map', '-x']
    for line in filter(match_sd, util.pread2(cmd).split('\n')):
        ids = line.split()
        SCSI[ids[6]] = ids
    return SCSI
