{
  for (  Short accountType : Arrays.asList(Account.ACCOUNT_TYPE_NORMAL,Account.ACCOUNT_TYPE_ADMIN,Account.ACCOUNT_TYPE_DOMAIN_ADMIN,Account.ACCOUNT_TYPE_RESOURCE_DOMAIN_ADMIN,Account.ACCOUNT_TYPE_PROJECT,(short)12345)) {
    Assert.assertEquals(RoleType.getAccountTypeByRole(null,accountType),accountType);
  }
}
