def __parseConfig(self):
    '\n        @Desc : Parses the configuration file passed and assigns\n        the parsed configuration\n        '
    try:
        if (self.__configFile is None):
            return FAILED
        self.__parsedConfig = configGenerator.getSetupConfig(self.__configFile)
        return SUCCESS
    except Exception as e:
        print ('\nException Occurred Under __parseConfig : %s' % GetDetailExceptionInfo(e))
        return FAILED
