{
  if (enable) {
    if (showControls) {
      buttonPanel.enableRemoteAccessControls(true);
    }
  }
 else {
    if (showControls) {
      buttonPanel.enableRemoteAccessControls(false);
    }
  }
}
