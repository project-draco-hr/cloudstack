{
  Insets insets=desktopScrollPane.getInsets();
  desktopScrollPane.setSize(vc.scaledWidth + 2 * Math.min(insets.left,insets.right),vc.scaledHeight + 2 * Math.min(insets.top,insets.bottom));
  vncFrame.pack();
}
