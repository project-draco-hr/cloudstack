{
  assert(clz != null);
  if (value != null) {
    try {
      return Enum.valueOf(clz,value.trim());
    }
 catch (    IllegalArgumentException ex) {
      assert(false);
    }
  }
  return null;
}
