{
  String args=" -d " + dev;
  if (privateGw != null) {
    args+=" -a " + rule;
    return executeInVR(routerIP,"vpc_privategw_acl.sh",args);
  }
 else {
    args+=" -i " + routerGIP;
    args+=" -m " + netmask;
    args+=" -a " + rule;
    return executeInVR(routerIP,"vpc_acl.sh",args);
  }
}
