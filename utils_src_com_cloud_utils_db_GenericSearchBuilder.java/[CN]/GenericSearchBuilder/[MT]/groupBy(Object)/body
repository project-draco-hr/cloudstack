{
  if (_groupBys == null) {
    _groupBys=new ArrayList<Attribute>();
  }
  Attribute[] attrs=_specifiedAttrs.toArray(new Attribute[_specifiedAttrs.size()]);
  for (  Attribute attr : attrs)   _groupBys.add(attr);
  _specifiedAttrs.clear();
  return this;
}
