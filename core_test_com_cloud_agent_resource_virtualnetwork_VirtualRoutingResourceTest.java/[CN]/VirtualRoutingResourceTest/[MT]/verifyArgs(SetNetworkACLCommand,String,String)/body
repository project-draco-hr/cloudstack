{
  _count++;
switch (_count) {
case 1:
    assertEquals(VRScripts.UPDATE_CONFIG,script);
  assertEquals(VRScripts.NETWORK_ACL_CONFIG,args);
break;
case 2:
assertEquals(VRScripts.UPDATE_CONFIG,script);
assertEquals(VRScripts.NETWORK_ACL_CONFIG,args);
break;
default :
fail();
}
}
