{
  stop=true;
  ServerSocket ss=serverSocket;
  if (ss != null) {
    try {
      ss.close();
    }
 catch (    IOException ioe) {
    }
  }
  Thread t=thread;
  if (t != null) {
    t.interrupt();
  }
}
