{
  if (this.levelMin != null && event.getLevel().isGreaterOrEqual(levelMin) == false) {
    return Filter.DENY;
  }
  if (this.levelMax != null && event.getLevel().toInt() > levelMax.toInt()) {
    return Filter.DENY;
  }
  if (acceptOnMatch) {
    return Filter.ACCEPT;
  }
 else {
    return Filter.NEUTRAL;
  }
}
