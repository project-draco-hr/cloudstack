{
  if (type != null) {
    return type;
  }
  if (guestIpType != null) {
    for (    GuestIpType gType : GuestIpType.values()) {
      if (gType.name().equalsIgnoreCase(guestIpType)) {
        return guestIpType;
      }
    }
  }
  return null;
}
