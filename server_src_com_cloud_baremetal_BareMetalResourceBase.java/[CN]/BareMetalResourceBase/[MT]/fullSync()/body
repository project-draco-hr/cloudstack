{
  Map<String,State> changes=new HashMap<String,State>();
  if (_vmName != null) {
    State state=getVmState();
    if (state != null) {
      changes.put(_vmName,state);
    }
  }
  return changes;
}
