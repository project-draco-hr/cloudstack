{
  VirtualMachine instance=vm.getVirtualMachine();
  if (network.getTrafficType() != Networks.TrafficType.Guest || instance.getType() == VirtualMachine.Type.DomainRouter) {
    return true;
  }
  return true;
}
