{
  String d=config.getAgentOvmRepoPath() + File.separator + ovmObject.deDash(storeUuid)+ File.separator+ config.getVirtualDiskDir()+ File.separator+ diskUuid;
  if (!d.endsWith(".raw")) {
    d=d.concat(".raw");
  }
  return d;
}
