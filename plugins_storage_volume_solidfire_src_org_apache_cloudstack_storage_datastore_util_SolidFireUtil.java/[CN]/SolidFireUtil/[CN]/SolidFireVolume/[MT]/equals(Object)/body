{
  if (!(obj instanceof SolidFireVolume)) {
    return false;
  }
  SolidFireVolume sfv=(SolidFireVolume)obj;
  if (_id == sfv._id && _name.equals(sfv._name) && _iqn.equals(sfv._iqn) && isActive() == sfv.isActive()) {
    return true;
  }
  return false;
}
