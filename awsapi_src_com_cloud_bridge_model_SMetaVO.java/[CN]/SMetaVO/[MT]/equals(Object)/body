{
  if (this == other)   return true;
  if (!(other instanceof SMetaVO))   return false;
  return getTarget().equals(((SMetaVO)other).getTarget()) && getTargetId() == ((SMetaVO)other).getTargetId() && getName().equals(((SMetaVO)other).getName());
}
