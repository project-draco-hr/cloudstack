{
  when(this.mockNwDao.acquireInLockTable(NW_ID,NetworkOrchestrationService.NetworkLockTimeout.value())).thenReturn(null);
  try {
    this.deployment.lock();
  }
  finally {
    verify(this.mockNwDao,times(1)).acquireInLockTable(NW_ID,600);
    assertNull(this.deployment.tableLockId);
  }
}
