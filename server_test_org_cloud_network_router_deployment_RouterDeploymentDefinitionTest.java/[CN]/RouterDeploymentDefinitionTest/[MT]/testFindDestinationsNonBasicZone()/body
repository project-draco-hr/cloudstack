{
  when(this.mockDataCenter.getNetworkType()).thenReturn(NetworkType.Advanced);
  List<DeployDestination> destinations=this.deployment.findDestinations();
  assertEquals(ONLY_THE_PROVIDED_AS_DEFAULT_DESTINATION_WAS_EXPECTED,1,destinations.size());
  assertEquals(ONLY_THE_PROVIDED_AS_DEFAULT_DESTINATION_WAS_EXPECTED,this.mockDestination,destinations.get(0));
}
