{
  if (null != param) {
    String parts[]=param.split(":");
    if (3 <= parts.length) {
      format=parts[0];
      zoneName=parts[1];
      osTypeName=parts[2];
      hypervisor=parts[3];
    }
  }
}
