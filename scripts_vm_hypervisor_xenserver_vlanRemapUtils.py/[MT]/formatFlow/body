def formatFlow(inPort, vlan, mac, outPut):
    flow = ('in_port=%s dl_vlan=%s dl_dst=%s idle_timeout=0 hard_timeout=0 \t\t\tactions=strip_vlan,output:%s' % (inPort, vlan, mac, outPut))
    return flow
