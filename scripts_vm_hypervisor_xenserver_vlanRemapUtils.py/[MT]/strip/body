def strip(str, direction='default'):
    str = str.lstrip().rstrip()
    if (direction == 'left'):
        return str[1:]
    if (direction == 'right'):
        return str[:(-1)]
    if (direction == 'both'):
        str = str[1:]
        str = str[:(-1)]
        return str
    return str
