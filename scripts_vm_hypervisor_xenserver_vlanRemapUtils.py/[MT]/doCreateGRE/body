def doCreateGRE(bridge, remoteIP, key):
    global result
    if (createGRE(bridge, remoteIP, key) < 0):
        log(('create GRE tunnel on %s for %s failed' % (bridge, remoteIP)))
    else:
        log(('WARNING: create GRE tunnel on %s for %s success' % (bridge, remoteIP)))
    print result
