def getInterfacesOnPort(nameOruuid):
    listPort = [vsctlPath, 'list port', nameOruuid]
    res = doCmd(listPort, True)
    for i in res:
        if ('interfaces' in i):
            (x, str) = i.split(':')
            str = strip(str, 'both')
            return str.split(',')
    return None
