{
  Boolean ret=false;
  if (_vmDisk > _vmDiskZero) {
    Integer oVmDisk=_vmDisk;
    _vmDisk=_vmDiskZero;
    ret=addDisk(image,"w");
    _vmDisk=oVmDisk;
  }
 else {
    ret=addDisk(image,"w");
  }
  return ret;
}
