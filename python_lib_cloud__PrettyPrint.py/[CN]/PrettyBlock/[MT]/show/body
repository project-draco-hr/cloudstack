def show(self, out):
    print  >> out, ('(BLOCK (width %d) (indent %d) (all %d) (broken %d)' % (self.width, self.indent, self.all, self.broken))
    for l in self.lines:
        l.show(out)
    print  >> out, ')'
