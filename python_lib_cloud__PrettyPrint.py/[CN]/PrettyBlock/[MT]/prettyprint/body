def prettyprint(self, line):
    self.indent = line.used
    line.block = self
    if (not line.fits(self.width)):
        self.broken = 1
    for l in self.lines:
        l.prettyprint(line)
    line.block = self.parent
