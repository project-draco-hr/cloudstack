{
  if (event == Event.STREAM_START) {
    if (verbose)     System.out.println("[" + this + "] INFO: Event "+ event+ " is received.");
    switchOn();
    onStart();
  }
 else   super.handleEvent(event,direction);
}
