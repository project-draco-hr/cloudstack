{
  for (  Object formatKey : ClipboardDataFormat.supportedTextBasedFormats)   if (serverClipboardDataFormats.containsKey(formatKey))   return serverClipboardDataFormats.get(formatKey);
  return null;
}
