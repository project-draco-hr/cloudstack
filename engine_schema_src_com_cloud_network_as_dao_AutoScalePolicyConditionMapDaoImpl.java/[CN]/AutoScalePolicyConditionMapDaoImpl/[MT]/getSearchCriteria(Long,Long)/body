{
  SearchCriteria<AutoScalePolicyConditionMapVO> sc=createSearchCriteria();
  if (policyId != null)   sc.addAnd("policyId",SearchCriteria.Op.EQ,policyId);
  if (conditionId != null)   sc.addAnd("conditionId",SearchCriteria.Op.EQ,conditionId);
  return sc;
}
