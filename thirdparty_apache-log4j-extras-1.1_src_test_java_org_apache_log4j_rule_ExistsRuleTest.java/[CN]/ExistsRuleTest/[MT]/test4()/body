{
  Stack stack=new Stack();
  stack.push("msg");
  Rule rule=ExistsRule.getRule(stack);
  assertEquals(0,stack.size());
  LoggingEvent event=new LoggingEvent("org.apache.log4j.Logger",Logger.getRootLogger(),System.currentTimeMillis(),Level.INFO,"",null);
  assertFalse(rule.evaluate(event,null));
}
