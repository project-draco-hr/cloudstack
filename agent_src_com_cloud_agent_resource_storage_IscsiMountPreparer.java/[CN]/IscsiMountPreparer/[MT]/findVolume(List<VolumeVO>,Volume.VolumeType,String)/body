{
  if (volumes == null)   return null;
  for (  final VolumeVO v : volumes) {
    if ((v.getVolumeType() == vType) && (v.getHostIp().equalsIgnoreCase(storageHost)))     return v;
  }
  return null;
}
