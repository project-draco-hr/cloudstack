{
  if (volumes == null)   return null;
  final List<VolumeVO> result=new ArrayList<VolumeVO>();
  for (  final VolumeVO v : volumes) {
    if (v.getVolumeType() == vType)     result.add(v);
  }
  return result;
}
