{
  if (!flyweight.containsKey(dc.getNetworkType())) {
    throw new IllegalArgumentException("The type given cannot be related to a NetworkTopology implementation. " + "Please, give a correct type.");
  }
  return flyweight.get(dc.getNetworkType());
}
