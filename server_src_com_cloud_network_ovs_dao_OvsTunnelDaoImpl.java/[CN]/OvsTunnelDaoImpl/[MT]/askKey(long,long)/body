{
  int key=-1;
  final Transaction txn=Transaction.currentTxn();
  txn.start();
  OvsTunnelVO t=lockByFromAndTo(from,to);
  if (t != null) {
    key=t.getKey();
    t.setKey(key + 1);
    update(t.getId(),t);
  }
  txn.commit();
  return key;
}
