def getDeviceByIp(self, ip):
    ips = CsDataBag('ips')
    dbag = ips.get_bag()
    for device in dbag:
        if (device == 'id'):
            continue
        for addy in dbag[device]:
            if (addy['public_ip'] == ip):
                return device
    return None
