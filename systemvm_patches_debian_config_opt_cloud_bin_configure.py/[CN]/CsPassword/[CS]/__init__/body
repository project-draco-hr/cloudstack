def __init__(self):
    db = dataBag()
    db.setKey('vmpassword')
    db.load()
    dbag = db.getDataBag()
    file = CsFile(self.cache)
    for item in dbag:
        if (item == 'id'):
            continue
        self.update(file, item, dbag[item])
    file.commit()
