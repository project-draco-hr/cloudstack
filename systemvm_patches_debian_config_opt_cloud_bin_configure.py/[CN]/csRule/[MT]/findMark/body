def findMark(self):
    srch = ('from all fwmark 0x%s lookup %s' % (self.tableNo, self.table))
    for i in csHelper().execute('ip rule show'):
        if (srch in i.strip()):
            return True
    return False
