def list(self):
    self.iplist = {}
    cmd = ('ip addr show dev ' + self.dev)
    for i in csHelper().execute(cmd):
        vals = i.lstrip().split()
        if (vals[0] == 'inet'):
            self.iplist[vals[1]] = self.dev
