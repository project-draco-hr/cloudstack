def __convert_to_dict(self, rule):
    rule = rule.replace('! -', '!_-')
    rule = rule.replace('-m state', '-m2 state')
    bits = rule.split(' ')
    rule = dict(zip(bits[0::2], bits[1::2]))
    if ('-A' in rule.keys()):
        self.chain = rule['-A']
    return rule
