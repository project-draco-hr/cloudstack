def validate_uploaded_template(self, up_templateid, templatestate):
    list_template_response = Template.list(self.apiclient, id=up_templateid)
    self.assertNotEqual(list_volume_response, None, 'Check if template exists in ListTemplates')
    self.assertEqual(list_template_response[0].state, templatestate, 'Check template state in List templates')
