def choose_category(fn):
    for (k, v) in known_categories.iteritems():
        if (k in fn):
            return v
    raise Exception(('Need to add a category for %s to %s:known_categories' % (fn, __file__)))
    sys.exit(1)
