{
  Object[] v=(Object[])callWrapper("get_ntp");
  int c=0;
  for (  Object o : v) {
    if (o instanceof java.lang.Boolean) {
      if (c == 0)       this.isServer=(Boolean)o;
      if (c == 1)       this.isRunning=(Boolean)o;
      if (c > 1)       return false;
      c+=1;
    }
 else     if (o instanceof java.lang.Object) {
      Object[] S=(Object[])o;
      for (      Object m : S) {
        this.addServer((String)m);
      }
    }
  }
  return true;
}
