{
  if (verbose)   System.out.println("[" + this + "] INFO: Buffer pushed back: "+ buf+ ".");
  if (cacheBuffer != null) {
    ByteBuffer tmp=cacheBuffer.join(buf);
    cacheBuffer.unref();
    cacheBuffer=tmp;
  }
 else {
    cacheBuffer=buf;
    cacheBuffer.ref();
  }
  resetCursor();
}
