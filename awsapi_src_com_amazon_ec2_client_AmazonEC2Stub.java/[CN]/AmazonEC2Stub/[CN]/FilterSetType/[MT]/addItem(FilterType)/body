{
  if (localItem == null) {
    localItem=new FilterType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(FilterType[])list.toArray(new FilterType[list.size()]);
}
