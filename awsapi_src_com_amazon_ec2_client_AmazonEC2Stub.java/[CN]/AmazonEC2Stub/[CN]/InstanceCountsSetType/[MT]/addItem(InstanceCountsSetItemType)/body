{
  if (localItem == null) {
    localItem=new InstanceCountsSetItemType[]{};
  }
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InstanceCountsSetItemType[])list.toArray(new InstanceCountsSetItemType[list.size()]);
}
