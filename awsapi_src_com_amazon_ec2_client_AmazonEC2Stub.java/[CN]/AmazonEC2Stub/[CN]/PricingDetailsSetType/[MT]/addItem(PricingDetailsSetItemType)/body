{
  if (localItem == null) {
    localItem=new PricingDetailsSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(PricingDetailsSetItemType[])list.toArray(new PricingDetailsSetItemType[list.size()]);
}
