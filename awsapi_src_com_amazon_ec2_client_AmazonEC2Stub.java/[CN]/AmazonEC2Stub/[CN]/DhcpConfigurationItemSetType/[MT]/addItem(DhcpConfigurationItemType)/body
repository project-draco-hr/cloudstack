{
  if (localItem == null) {
    localItem=new DhcpConfigurationItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(DhcpConfigurationItemType[])list.toArray(new DhcpConfigurationItemType[list.size()]);
}
