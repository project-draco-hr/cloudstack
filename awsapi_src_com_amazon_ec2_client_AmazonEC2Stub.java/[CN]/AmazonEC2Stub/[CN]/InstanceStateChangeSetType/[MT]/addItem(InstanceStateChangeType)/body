{
  if (localItem == null) {
    localItem=new InstanceStateChangeType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InstanceStateChangeType[])list.toArray(new InstanceStateChangeType[list.size()]);
}
