{
  if (localItem == null) {
    localItem=new RegionItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(RegionItemType[])list.toArray(new RegionItemType[list.size()]);
}
