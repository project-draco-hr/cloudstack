{
  if (localItem == null) {
    localItem=new MonitorInstancesSetItemType[]{};
  }
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(MonitorInstancesSetItemType[])list.toArray(new MonitorInstancesSetItemType[list.size()]);
}
