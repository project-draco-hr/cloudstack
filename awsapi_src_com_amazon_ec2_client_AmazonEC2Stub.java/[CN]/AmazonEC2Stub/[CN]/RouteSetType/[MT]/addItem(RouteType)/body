{
  if (localItem == null) {
    localItem=new RouteType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(RouteType[])list.toArray(new RouteType[list.size()]);
}
