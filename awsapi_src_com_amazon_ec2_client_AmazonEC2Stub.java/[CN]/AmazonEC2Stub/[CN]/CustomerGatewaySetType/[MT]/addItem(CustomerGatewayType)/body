{
  if (localItem == null) {
    localItem=new CustomerGatewayType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(CustomerGatewayType[])list.toArray(new CustomerGatewayType[list.size()]);
}
