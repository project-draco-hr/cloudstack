{
  if (localItem == null) {
    localItem=new VpnConnectionType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VpnConnectionType[])list.toArray(new VpnConnectionType[list.size()]);
}
