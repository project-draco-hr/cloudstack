{
  if (localItem == null) {
    localItem=new VolumeStatusEventItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VolumeStatusEventItemType[])list.toArray(new VolumeStatusEventItemType[list.size()]);
}
