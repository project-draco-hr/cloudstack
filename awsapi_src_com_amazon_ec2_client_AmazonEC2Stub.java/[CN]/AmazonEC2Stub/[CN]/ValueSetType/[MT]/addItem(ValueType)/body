{
  if (localItem == null) {
    localItem=new ValueType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(ValueType[])list.toArray(new ValueType[list.size()]);
}
