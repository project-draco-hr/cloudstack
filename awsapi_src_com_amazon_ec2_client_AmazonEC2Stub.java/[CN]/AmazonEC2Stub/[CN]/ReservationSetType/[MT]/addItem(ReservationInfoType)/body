{
  if (localItem == null) {
    localItem=new ReservationInfoType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(ReservationInfoType[])list.toArray(new ReservationInfoType[list.size()]);
}
