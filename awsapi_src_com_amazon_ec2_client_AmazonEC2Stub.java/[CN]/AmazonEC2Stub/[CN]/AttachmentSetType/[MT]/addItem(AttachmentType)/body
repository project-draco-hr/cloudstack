{
  if (localItem == null) {
    localItem=new AttachmentType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(AttachmentType[])list.toArray(new AttachmentType[list.size()]);
}
