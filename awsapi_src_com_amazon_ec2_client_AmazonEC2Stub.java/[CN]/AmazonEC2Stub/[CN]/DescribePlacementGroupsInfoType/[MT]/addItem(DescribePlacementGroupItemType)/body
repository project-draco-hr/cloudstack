{
  if (localItem == null) {
    localItem=new DescribePlacementGroupItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(DescribePlacementGroupItemType[])list.toArray(new DescribePlacementGroupItemType[list.size()]);
}
