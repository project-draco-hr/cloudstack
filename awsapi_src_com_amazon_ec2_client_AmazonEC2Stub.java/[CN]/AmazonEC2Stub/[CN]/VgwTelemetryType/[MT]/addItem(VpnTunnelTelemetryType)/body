{
  if (localItem == null) {
    localItem=new VpnTunnelTelemetryType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VpnTunnelTelemetryType[])list.toArray(new VpnTunnelTelemetryType[list.size()]);
}
