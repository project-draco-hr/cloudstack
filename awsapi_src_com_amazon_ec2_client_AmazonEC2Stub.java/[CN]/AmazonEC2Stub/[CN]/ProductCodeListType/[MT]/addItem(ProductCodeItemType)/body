{
  if (localItem == null) {
    localItem=new ProductCodeItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(ProductCodeItemType[])list.toArray(new ProductCodeItemType[list.size()]);
}
