{
  if (localItem == null) {
    localItem=new VolumeStatusDetailsItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VolumeStatusDetailsItemType[])list.toArray(new VolumeStatusDetailsItemType[list.size()]);
}
