{
  if (localItem == null) {
    localItem=new RouteTableType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(RouteTableType[])list.toArray(new RouteTableType[list.size()]);
}
