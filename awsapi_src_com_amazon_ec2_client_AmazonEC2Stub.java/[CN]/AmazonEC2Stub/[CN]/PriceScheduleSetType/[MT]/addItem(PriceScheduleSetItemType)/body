{
  if (localItem == null) {
    localItem=new PriceScheduleSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(PriceScheduleSetItemType[])list.toArray(new PriceScheduleSetItemType[list.size()]);
}
