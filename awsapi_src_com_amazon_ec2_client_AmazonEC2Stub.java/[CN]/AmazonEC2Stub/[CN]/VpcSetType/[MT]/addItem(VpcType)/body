{
  if (localItem == null) {
    localItem=new VpcType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VpcType[])list.toArray(new VpcType[list.size()]);
}
