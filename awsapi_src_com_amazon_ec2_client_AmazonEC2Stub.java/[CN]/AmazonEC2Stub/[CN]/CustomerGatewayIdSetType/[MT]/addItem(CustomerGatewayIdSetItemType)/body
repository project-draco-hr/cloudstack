{
  if (localItem == null) {
    localItem=new CustomerGatewayIdSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(CustomerGatewayIdSetItemType[])list.toArray(new CustomerGatewayIdSetItemType[list.size()]);
}
