{
  if (localItem == null) {
    localItem=new ExportTaskResponseType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(ExportTaskResponseType[])list.toArray(new ExportTaskResponseType[list.size()]);
}
