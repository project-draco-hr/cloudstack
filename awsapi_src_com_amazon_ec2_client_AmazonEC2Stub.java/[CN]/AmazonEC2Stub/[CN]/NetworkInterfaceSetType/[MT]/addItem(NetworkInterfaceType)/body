{
  if (localItem == null) {
    localItem=new NetworkInterfaceType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(NetworkInterfaceType[])list.toArray(new NetworkInterfaceType[list.size()]);
}
