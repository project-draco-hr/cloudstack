{
  if (localItem == null) {
    localItem=new DhcpValueType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(DhcpValueType[])list.toArray(new DhcpValueType[list.size()]);
}
