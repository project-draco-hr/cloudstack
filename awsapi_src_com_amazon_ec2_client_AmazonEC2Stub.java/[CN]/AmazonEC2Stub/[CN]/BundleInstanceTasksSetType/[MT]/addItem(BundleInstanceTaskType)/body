{
  if (localItem == null) {
    localItem=new BundleInstanceTaskType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(BundleInstanceTaskType[])list.toArray(new BundleInstanceTaskType[list.size()]);
}
