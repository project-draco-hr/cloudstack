{
  if (localItem == null) {
    localItem=new InstanceStatusEventType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InstanceStatusEventType[])list.toArray(new InstanceStatusEventType[list.size()]);
}
