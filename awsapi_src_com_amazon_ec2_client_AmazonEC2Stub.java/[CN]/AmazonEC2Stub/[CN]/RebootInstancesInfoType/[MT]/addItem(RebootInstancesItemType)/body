{
  if (localItem == null) {
    localItem=new RebootInstancesItemType[]{};
  }
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(RebootInstancesItemType[])list.toArray(new RebootInstancesItemType[list.size()]);
}
