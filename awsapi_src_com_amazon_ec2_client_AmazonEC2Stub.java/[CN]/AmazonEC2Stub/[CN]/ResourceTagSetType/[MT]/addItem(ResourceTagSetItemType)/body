{
  if (localItem == null) {
    localItem=new ResourceTagSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(ResourceTagSetItemType[])list.toArray(new ResourceTagSetItemType[list.size()]);
}
