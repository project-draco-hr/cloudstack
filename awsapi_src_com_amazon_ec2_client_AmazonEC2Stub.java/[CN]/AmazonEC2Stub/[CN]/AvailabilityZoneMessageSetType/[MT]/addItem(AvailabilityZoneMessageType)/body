{
  if (localItem == null) {
    localItem=new AvailabilityZoneMessageType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(AvailabilityZoneMessageType[])list.toArray(new AvailabilityZoneMessageType[list.size()]);
}
