{
  if (localItem == null) {
    localItem=new VpnGatewayType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(VpnGatewayType[])list.toArray(new VpnGatewayType[list.size()]);
}
