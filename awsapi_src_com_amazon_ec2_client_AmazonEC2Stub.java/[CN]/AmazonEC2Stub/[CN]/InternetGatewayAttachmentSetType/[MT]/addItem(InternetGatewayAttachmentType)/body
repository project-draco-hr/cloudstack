{
  if (localItem == null) {
    localItem=new InternetGatewayAttachmentType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InternetGatewayAttachmentType[])list.toArray(new InternetGatewayAttachmentType[list.size()]);
}
