{
  if (localItem == null) {
    localItem=new InternetGatewayType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InternetGatewayType[])list.toArray(new InternetGatewayType[list.size()]);
}
