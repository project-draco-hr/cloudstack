{
  if (localItem == null) {
    localItem=new InstancePrivateIpAddressesSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(InstancePrivateIpAddressesSetItemType[])list.toArray(new InstancePrivateIpAddressesSetItemType[list.size()]);
}
