{
  if (localItem == null) {
    localItem=new GroupItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(GroupItemType[])list.toArray(new GroupItemType[list.size()]);
}
