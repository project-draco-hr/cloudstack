{
  if (localItem == null) {
    localItem=new LicenseSetItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(LicenseSetItemType[])list.toArray(new LicenseSetItemType[list.size()]);
}
