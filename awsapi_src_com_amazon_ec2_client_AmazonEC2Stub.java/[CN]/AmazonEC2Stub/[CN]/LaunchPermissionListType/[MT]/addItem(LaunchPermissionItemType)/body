{
  if (localItem == null) {
    localItem=new LaunchPermissionItemType[]{};
  }
  localItemTracker=true;
  java.util.List list=org.apache.axis2.databinding.utils.ConverterUtil.toList(localItem);
  list.add(param);
  this.localItem=(LaunchPermissionItemType[])list.toArray(new LaunchPermissionItemType[list.size()]);
}
