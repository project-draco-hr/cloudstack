{
  if (instance instanceof Advised) {
    try {
      return (T)((Advised)instance).getTargetSource().getTarget();
    }
 catch (    Exception e) {
      return (T)instance;
    }
  }
  return (T)instance;
}
