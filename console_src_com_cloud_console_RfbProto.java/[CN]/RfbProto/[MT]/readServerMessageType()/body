{
  int msgType=is.readUnsignedByte();
  if (rec != null) {
    if (msgType == Bell) {
      rec.writeByte(msgType);
      if (numUpdatesInSession > 0)       rec.flush();
    }
  }
  return msgType;
}
