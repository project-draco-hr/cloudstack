{
  if (this == o) {
    return 0;
  }
  if (!(o instanceof UUID)) {
    throw new ClassCastException("The argument must be of type '" + getClass().getName() + "'.");
  }
  long time=NumbersUtil.bytesToLong(_uuid,12);
  UUID t=(UUID)o;
  long ttime=NumbersUtil.bytesToLong(t._uuid,12);
  if (time > ttime) {
    return 1;
  }
  if (time < ttime) {
    return -1;
  }
  return 0;
}
