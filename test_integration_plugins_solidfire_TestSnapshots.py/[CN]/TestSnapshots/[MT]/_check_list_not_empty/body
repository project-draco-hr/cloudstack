def _check_list_not_empty(self, in_list):
    self.assertEqual(isinstance(in_list, list), True, "'in_list' is not a list.")
    self.assertGreater(len(in_list), 0, "The size of 'in_list' must be greater than zero.")
