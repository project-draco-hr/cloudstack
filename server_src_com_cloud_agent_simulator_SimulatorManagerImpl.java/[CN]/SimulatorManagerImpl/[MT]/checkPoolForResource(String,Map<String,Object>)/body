{
  for (  Entry<AgentResourceBase,Map<String,String>> res : _resources.entrySet()) {
    if (res.getKey().getName().equalsIgnoreCase(name)) {
      s_logger.info("server resource: " + name + " already exists. reconnecting ...");
      return true;
    }
  }
  return false;
}
