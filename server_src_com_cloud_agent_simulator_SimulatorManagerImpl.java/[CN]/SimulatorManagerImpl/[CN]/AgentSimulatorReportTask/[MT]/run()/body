{
  try {
    int size=_hostDao.listBy(Host.Type.Routing,getZone()).size();
    s_logger.info("Simulator running with: " + size + " hosts in zone: "+ getZone());
  }
 catch (  Throwable e) {
    s_logger.error("Unexpected exception " + e.getMessage(),e);
  }
}
