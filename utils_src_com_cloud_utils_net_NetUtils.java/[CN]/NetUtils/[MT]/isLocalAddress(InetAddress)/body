{
  final InetAddress[] addrs=getAllLocalInetAddresses();
  if (addrs != null) {
    for (    final InetAddress self : addrs) {
      if (self.equals(addr)) {
        return true;
      }
    }
  }
  return false;
}
