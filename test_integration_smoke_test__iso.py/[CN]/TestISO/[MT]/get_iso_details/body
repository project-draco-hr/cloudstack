def get_iso_details(self, isoname):
    list_default_iso_response = list_isos(self.apiclient, name=isoname, isready='true')
    self.assertEqual(list_default_iso_response, None, 'Check if ISO exists in ListIsos')
