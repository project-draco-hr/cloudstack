def merge(dbag, staticroutes):
    for route in staticroutes['routes']:
        key = route['network']
        revoke = route['revoke']
        if revoke:
            try:
                del dbag[key]
            except KeyError:
                pass
        else:
            dbag[key] = route
    return dbag
