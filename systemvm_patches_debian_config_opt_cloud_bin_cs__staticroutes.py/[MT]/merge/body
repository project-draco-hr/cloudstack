def merge(dbag, staticroutes):
    for route in staticroutes['routes']:
        key = route['ip_address']
        revoke = route['revoke']
        if revoke:
            del dbag[key]
        else:
            dbag[key] = route
    return dbag
