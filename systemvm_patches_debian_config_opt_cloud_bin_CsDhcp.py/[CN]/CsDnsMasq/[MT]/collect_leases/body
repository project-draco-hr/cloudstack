def collect_leases(self):
    try:
        for line in open(LEASES):
            bits = line.strip().split(' ')
            to = {'device': bits[0], 'mac': bits[1], 'ip': bits[2], 'host': bits[3], }
            self.leases.append(to)
    except IOError:
        pass
