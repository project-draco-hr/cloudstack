@attr(tags=['advanced'])
@log_test_exceptions
def test_07_egress_fr7(self):
    'Test Create Egress rule without End Port\n        '
    self.create_vm()
    self.createEgressRule(protocol='tcp', start_port=80)
    self.exec_script_on_user_vm(' wget -t1 http://apache.claz.org/favicon.ico', "| grep -oP 'failed:'", "['failed:']", negative_test=False)
