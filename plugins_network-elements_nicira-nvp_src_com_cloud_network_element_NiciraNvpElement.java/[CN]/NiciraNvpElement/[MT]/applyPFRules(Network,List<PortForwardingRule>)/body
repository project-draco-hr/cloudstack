{
  s_logger.debug("Entering applyPFRules");
  for (  PortForwardingRule rule : rules) {
    s_logger.debug("PortForwardingRule rule : from " + rule.getSourceIpAddressId() + " to "+ rule.getDestinationIpAddress().addr()+ " port "+ rule.getDestinationPortStart()+ "-"+ rule.getDestinationPortEnd());
  }
  return false;
}
