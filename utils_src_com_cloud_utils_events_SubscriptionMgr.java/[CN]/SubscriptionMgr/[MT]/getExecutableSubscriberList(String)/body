{
  List<SubscriberInfo> l=registry.get(subject);
  if (l != null) {
    ArrayList<SubscriberInfo> clonedList=new ArrayList<SubscriberInfo>(l.size());
    for (    SubscriberInfo info : l)     clonedList.add(info);
    return clonedList;
  }
  return null;
}
