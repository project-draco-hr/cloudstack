{
  if (getDevice() != null && getDevice().contains(":")) {
    String spl[]=getDevice().split(":");
    setHost(spl[0]);
    setMountPoint(spl[1]);
  }
 else {
    return null;
  }
  return (String)_fs.get("Host");
}
