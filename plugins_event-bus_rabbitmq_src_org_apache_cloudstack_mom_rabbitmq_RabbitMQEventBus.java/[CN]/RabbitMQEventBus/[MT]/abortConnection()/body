{
  if (_connection == null)   return;
  try {
    _connection.abort();
  }
 catch (  Exception e) {
    s_logger.warn("Failed to abort connection due to " + e.getMessage());
  }
  _connection=null;
}
