{
  if (s_connection == null)   return;
  try {
    s_connection.abort();
  }
 catch (  Exception e) {
    logger.warn("Failed to abort connection due to " + e.getMessage());
  }
  s_connection=null;
}
