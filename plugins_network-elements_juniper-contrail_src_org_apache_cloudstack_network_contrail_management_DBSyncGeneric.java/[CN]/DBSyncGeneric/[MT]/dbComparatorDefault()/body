{
  Comparator comparator=new Comparator<Identity>(){
    @Override public int compare(    Identity u1,    Identity u2){
      return u1.getUuid().compareTo(u2.getUuid());
    }
  }
;
  return comparator;
}
