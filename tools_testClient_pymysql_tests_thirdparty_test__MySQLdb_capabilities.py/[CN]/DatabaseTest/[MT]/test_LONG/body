def test_LONG(self):

    def generator(row, col):
        if (col == 0):
            return row
        else:
            return self.BLOBUText
    self.check_data_integrity(('col1 INT', 'col2 LONG'), generator)
