{
  Connection conn=_connPool.slaveConnect(_host.ip,_username,_password);
  if (conn == null) {
    return false;
  }
 else {
    try {
      Session.localLogout(conn);
    }
 catch (    Exception e) {
    }
    conn.dispose();
  }
  return true;
}
