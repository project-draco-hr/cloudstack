def __init__(self, host='localhost', port=3306, user='cloud', passwd='cloud', db='cloud'):
    self.host = host
    self.port = port
    self.user = str(user)
    self.passwd = passwd
    self.database = db
