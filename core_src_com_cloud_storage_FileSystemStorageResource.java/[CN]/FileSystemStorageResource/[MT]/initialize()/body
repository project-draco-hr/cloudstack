{
  StartupCommand[] cmds=super.initialize();
  StartupStorageCommand cmd=(StartupStorageCommand)cmds[0];
  initLocalStorage(cmd);
  cmd.setTemplateInfo(new HashMap<String,TemplateInfo>());
  return new StartupCommand[]{cmd};
}
