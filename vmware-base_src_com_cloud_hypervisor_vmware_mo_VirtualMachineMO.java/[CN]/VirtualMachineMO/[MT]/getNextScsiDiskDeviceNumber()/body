{
  int scsiControllerKey=getScsiDeviceControllerKey();
  int deviceNumber=getNextDeviceNumber(scsiControllerKey);
  if (VmwareHelper.isReservedScsiDeviceNumber(deviceNumber))   deviceNumber++;
  return deviceNumber;
}
