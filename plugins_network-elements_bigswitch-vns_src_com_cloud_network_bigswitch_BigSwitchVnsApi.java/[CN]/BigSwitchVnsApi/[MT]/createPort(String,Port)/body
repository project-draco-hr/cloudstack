{
  String uri=_nsBaseUri + "/tenant/" + port.getTenant_id()+ "/networks/"+ networkUuid+ "/ports";
  executeCreateObject(port,new TypeToken<Port>(){
  }
.getType(),uri,Collections.<String,String>emptyMap());
}
