def getThisHost():
    uuid = None
    f = open('/etc/xensource-inventory', 'r')
    for line in f.readlines():
        if line.startswith('INSTALLATION_UUID'):
            uuid = line.split("'")[1]
    f.close()
    return uuid
