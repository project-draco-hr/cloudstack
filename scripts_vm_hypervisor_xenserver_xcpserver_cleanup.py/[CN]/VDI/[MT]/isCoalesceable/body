def isCoalesceable(self):
    'A VDI is coalesceable if it has no siblings and is not a leaf'
    return ((not self.scanError) and self.parent and (len(self.parent.children) == 1) and self.hidden and (len(self.children) > 0))
