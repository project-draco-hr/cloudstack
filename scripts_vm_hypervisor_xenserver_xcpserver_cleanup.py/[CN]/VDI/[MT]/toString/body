def toString(self):
    strHidden = ''
    if self.hidden:
        strHidden = '*'
    strSizeVHD = ''
    if (self._sizeVHD > 0):
        strSizeVHD = Util.num2str(self._sizeVHD)
    return ('%s%s(%s/%s)' % (strHidden, self.uuid[0:8], Util.num2str(self.sizeVirt), strSizeVHD))
