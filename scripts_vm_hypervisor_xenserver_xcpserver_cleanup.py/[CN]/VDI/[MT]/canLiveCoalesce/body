def canLiveCoalesce(self):
    'Can we stop-and-leaf-coalesce this VDI? The VDI must be\n        isLeafCoalesceable() already'
    return ((self.getSizeVHD() <= self.LIVE_LEAF_COALESCE_MAX_SIZE) or (self.getConfig(self.DB_LEAFCLSC) == self.LEAFCLSC_FORCE))
