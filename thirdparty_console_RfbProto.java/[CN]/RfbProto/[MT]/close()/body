{
  try {
    if (sock != null && !closed) {
      sock.close();
      sock=null;
      closed=true;
      Logger.log(Logger.INFO,"RFB socket closed");
    }
    if (rec != null) {
      rec.close();
      rec=null;
    }
  }
 catch (  Exception e) {
  }
}
