{
  if (this.tableLockId != null) {
    vpcDao.releaseFromLockTable(this.tableLockId);
    if (logger.isDebugEnabled()) {
      logger.debug("Lock is released for vpc id " + this.tableLockId + " as a part of router startup in "+ dest);
    }
  }
}
