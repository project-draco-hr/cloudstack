import paramiko
import time
import cloudstackException
import contextlib
import logging
from contextlib import closing
if (__name__ == '__main__'):
    with contextlib.closing(remoteSSHClient('10.223.75.10', 22, 'root', 'password')) as ssh:
        print ssh.execute('ls -l')
