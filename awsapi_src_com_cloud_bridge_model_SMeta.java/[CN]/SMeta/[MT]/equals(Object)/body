{
  if (this == other)   return true;
  if (!(other instanceof SMeta))   return false;
  return getTarget().equals(((SMeta)other).getTarget()) && getTargetId() == ((SMeta)other).getTargetId() && getName().equals(((SMeta)other).getName());
}
