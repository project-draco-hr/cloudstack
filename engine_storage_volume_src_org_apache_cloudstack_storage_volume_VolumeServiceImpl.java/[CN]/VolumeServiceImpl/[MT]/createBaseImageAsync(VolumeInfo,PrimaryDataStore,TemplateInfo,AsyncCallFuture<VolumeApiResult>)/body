{
  CreateBaseImageContext<VolumeApiResult> context=new CreateBaseImageContext<VolumeApiResult>(null,volume,dataStore,template,future);
  AsyncCallbackDispatcher<VolumeServiceImpl,CreateBaseImageResult> caller=AsyncCallbackDispatcher.create(this);
  caller.setCallback(caller.getTarget().createBaseImageCallback(null,null)).setContext(context);
  templateInstallStrategy.installAsync(template,dataStore,caller);
}
