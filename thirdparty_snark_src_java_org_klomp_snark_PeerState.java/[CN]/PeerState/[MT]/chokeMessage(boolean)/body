{
  log.log(Level.FINEST,peer + " rcv " + (choke ? "" : "un")+ "choked");
  choked=choke;
  if (choked) {
    resend=true;
  }
  listener.gotChoke(peer,choke);
  if (!choked && interesting) {
    request();
  }
}
