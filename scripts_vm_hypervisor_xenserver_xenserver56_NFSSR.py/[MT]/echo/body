def echo(fn):

    def wrapped(*v, **k):
        name = fn.__name__
        util.SMlog(('#### NFS enter  %s ####' % name))
        res = fn(*v, **k)
        util.SMlog(('#### NFS exit  %s ####' % name))
        return res
    return wrapped
