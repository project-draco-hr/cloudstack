def child_at(sxpr, index, val=None):
    'Get the child at the given index (zero-based).\n\n    sxpr  sxpr\n    index index\n    val   default value\n    '
    kids = children(sxpr)
    if (len(kids) > index):
        val = kids[index]
    return val
