def from_string(s):
    'Create an sxpr by parsing a string.\n\n    s string\n    returns sxpr\n    '
    if (s == ''):
        return []
    io = StringIO(s)
    vals = parse(io)
    if (vals is []):
        return None
    else:
        return vals[0]
