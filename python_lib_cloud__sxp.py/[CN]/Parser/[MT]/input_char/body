def input_char(self, c):
    if self.at_eof():
        pass
    elif (c == '\n'):
        self.line_no += 1
        self.char_no = 0
    else:
        self.char_no += 1
    if (self.state is None):
        self.begin_start(None)
    self.state.fn(c)
