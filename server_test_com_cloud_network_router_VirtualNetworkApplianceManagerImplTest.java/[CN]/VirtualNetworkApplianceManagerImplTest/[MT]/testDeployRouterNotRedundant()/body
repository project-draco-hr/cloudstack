{
  DataCenter dc=new DataCenterVO(1L,"name","description","dns",null,null,null,"cidr","domain",null,NetworkType.Basic,"zoneToken","domainSuffix");
  when(_routerDao.getNextInSequence(Long.class,"id")).thenReturn(1L);
  when(_resourceMgr.getDefaultHypervisor(1L)).thenReturn(HypervisorType.Any);
  DeploymentPlan plan=new DataCenterDeployment(1L);
  VirtualRouter vr=virtualNetworkApplianceManagerImpl.deployRouter(new AccountVO(1L),new DeployDestination(dc,null,null,null),plan,null,false,new VirtualRouterProviderVO(),0L,null,new LinkedHashMap<Network,List<? extends NicProfile>>(),true,null);
  assertEquals(vr,null);
}
