def _verifySfVolumeIds(self, src_sf_volume, dest_sf_volume):
    self.assert_((src_sf_volume['volumeID'] < dest_sf_volume['volumeID']), "The destination SolidFire root volume's ID should be greater than the id of the source one.")
