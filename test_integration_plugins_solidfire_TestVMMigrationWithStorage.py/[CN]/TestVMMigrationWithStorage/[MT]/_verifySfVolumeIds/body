def _verifySfVolumeIds(self, src_sf_volume, dest_sf_volume):
    self.assert_((src_sf_volume.volume_id < dest_sf_volume.volume_id), "The destination SolidFire root volume's ID should be greater than the id of the source one.")
