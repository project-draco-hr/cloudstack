{
  if (this == that) {
    return true;
  }
 else   if (that instanceof BaseType) {
    BaseType th=(BaseType)that;
    if (toString().equalsIgnoreCase(th.toString())) {
      return true;
    }
  }
  return false;
}
