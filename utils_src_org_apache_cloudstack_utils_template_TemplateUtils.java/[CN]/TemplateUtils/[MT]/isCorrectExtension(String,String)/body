{
  if (url.toLowerCase().endsWith(ext) || url.toLowerCase().endsWith(ext + ".gz") || url.toLowerCase().endsWith(ext + ".bz2")|| url.toLowerCase().endsWith(ext + ".zip")) {
    return true;
  }
  return false;
}
