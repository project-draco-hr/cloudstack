def _bool_from_xml(n):
    s = _str_from_xml(n)
    if (s == 'True'):
        return True
    elif (s == 'False'):
        return False
    else:
        raise Error(('Unknown boolean value %s' % s))
