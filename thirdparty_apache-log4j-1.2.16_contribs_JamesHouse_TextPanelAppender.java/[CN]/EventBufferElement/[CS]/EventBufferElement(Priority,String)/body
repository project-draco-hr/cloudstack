{
  this.prio=prio;
  this.text=text;
  numLines=1;
  int pos=pos=text.indexOf('\n',0);
  int len=text.length() - 1;
  while ((pos > 0) && (pos < len))   numLines++;
  pos=text.indexOf('\n',pos + 1);
}
