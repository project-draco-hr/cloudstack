{
  if (this.routers.size() >= 5) {
    logger.error("Too many redundant routers!");
  }
  int routersExpected=1;
  if (this.isRedundant) {
    routersExpected=2;
  }
  return routersExpected < this.routers.size() ? 0 : routersExpected - this.routers.size();
}
