{
  if (this.isBasic()) {
    this.routers=routerDao.listByNetworkAndPodAndRole(this.guestNetwork.getId(),this.getPodId(),Role.VIRTUAL_ROUTER);
  }
 else {
    this.routers=routerDao.listByNetworkAndRole(this.guestNetwork.getId(),Role.VIRTUAL_ROUTER);
  }
}
