{
  if (def == null)   return;
  if (!shouldLoad(def)) {
    return;
  }
  with.with(def,parents);
  parents.push(def);
  for (  ModuleDefinition child : def.getChildren()) {
    withModule(child,parents,with);
  }
  parents.pop();
}
