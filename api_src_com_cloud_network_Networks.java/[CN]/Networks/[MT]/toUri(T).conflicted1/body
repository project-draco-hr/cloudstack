{
  try {
    if (value.toString().contains(":"))     return new URI(value.toString());
 else     return new URI(scheme,value.toString(),null);
  }
 catch (  URISyntaxException e) {
    throw new CloudRuntimeException("Unable to convert to isolation type URI: " + value);
  }
}
