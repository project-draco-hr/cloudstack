{
  String accountIdStr=String.valueOf(network.getAccountId());
  String networkUUIDStr=String.valueOf(network.getId());
  return Networks.BroadcastDomainType.Mido.toUri(accountIdStr + "." + networkUUIDStr+ ":"+ networkUUIDStr);
}
