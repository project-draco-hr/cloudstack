{
  Map<String,List<String>> parentResults=_parentLocator != null ? _parentLocator.getAllAccessibleAdapters() : new HashMap<String,List<String>>();
  Map<String,List<String>> results=getAdapters();
  parentResults.putAll(results);
  return parentResults;
}
