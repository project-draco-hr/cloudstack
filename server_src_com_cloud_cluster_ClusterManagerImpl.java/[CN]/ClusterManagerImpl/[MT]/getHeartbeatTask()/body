{
  return new Runnable(){
    @Override public void run(){
      if (s_logger.isTraceEnabled())       s_logger.trace("Cluster manager heartbeat update, id:" + _mshostId);
      _mshostDao.update(_mshostId,DateUtil.currentGMTTime());
      peerScan();
    }
  }
;
}
