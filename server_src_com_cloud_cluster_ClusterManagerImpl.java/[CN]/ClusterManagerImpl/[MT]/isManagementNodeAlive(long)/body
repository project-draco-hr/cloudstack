{
  ManagementServerHostVO mshost=_mshostDao.findById(msid);
  if (mshost != null) {
    if (mshost.getLastUpdateTime().getTime() >= DateUtil.currentGMTTime().getTime() - heartbeatThreshold)     return true;
  }
  return false;
}
