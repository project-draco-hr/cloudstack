{
  if (this == other)   return true;
  if (!(other instanceof MHostMount))   return false;
  return getMhost().equals(((MHostMount)other).getMhost()) && getShost().equals(((MHostMount)other).getShost());
}
