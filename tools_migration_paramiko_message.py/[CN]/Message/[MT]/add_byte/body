def add_byte(self, b):
    '\n        Write a single byte to the stream, without any formatting.\n        \n        @param b: byte to add\n        @type b: str\n        '
    self.packet.write(b)
    return self
