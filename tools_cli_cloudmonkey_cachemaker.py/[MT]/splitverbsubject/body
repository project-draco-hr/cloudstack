def splitverbsubject(string):
    idx = 0
    for char in string:
        if char.islower():
            idx += 1
        else:
            break
    return (string[:idx].lower(), string[idx:].lower())
