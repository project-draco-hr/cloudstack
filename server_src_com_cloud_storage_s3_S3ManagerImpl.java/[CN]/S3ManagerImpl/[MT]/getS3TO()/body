{
  final List<S3VO> s3s=this.s3Dao.listAll();
  if (s3s == null || (s3s != null && s3s.isEmpty())) {
    return null;
  }
  if (s3s.size() == 1) {
    return s3s.get(0).toS3TO();
  }
  throw new CloudRuntimeException("Multiple S3 instances have been " + "defined.  Only one instance configuration is supported.");
}
