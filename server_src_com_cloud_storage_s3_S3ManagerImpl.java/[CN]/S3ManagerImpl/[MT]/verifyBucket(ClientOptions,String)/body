{
  if (!doesBucketExist(clientOptions,bucketName)) {
    return singletonList(format("Bucket %1$s does not exist.",bucketName));
  }
  if (!canReadWriteBucket(clientOptions,bucketName)) {
    return singletonList(format("Can read/write from bucket %1$s.",bucketName));
  }
  return emptyList();
}
