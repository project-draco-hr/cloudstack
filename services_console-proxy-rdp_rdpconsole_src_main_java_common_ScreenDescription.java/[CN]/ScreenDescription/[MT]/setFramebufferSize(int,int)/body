{
  if (height <= 0 || width <= 0)   throw new RuntimeException("Incorrect framebuffer size: " + width + "x"+ height+ ".");
  this.framebufferWidth=width;
  this.framebufferHeight=height;
  callSizeChangeListeners(width,height);
}
