def __sendGetReqToCS(self, url, payload):
    '\n        @Name : __sendGetReqToCS\n        @Desc : Sends the GET Request to CS\n        @Input : url: URL to send post req\n                 payload:Payload information as part of request\n        '
    try:
        response = requests.get(url, params=payload, cert=self.certPath, verify=self.httpsFlag)
        return response
    except Exception as e:
        self.logger.exception(('__sendGetReqToCS : Exception Occurred: %s' % GetDetailExceptionInfo(e)))
        return FAILED
