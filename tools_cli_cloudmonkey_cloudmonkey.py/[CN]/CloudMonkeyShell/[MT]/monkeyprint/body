def monkeyprint(self, *args):
    output = ''
    try:
        for arg in args:
            if isinstance(type(arg), types.NoneType):
                continue
            output += str(arg)
    except Exception as e:
        print e
    if (self.color == 'true'):
        monkeyprint(output)
    else:
        print output
