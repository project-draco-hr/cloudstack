def pipe_runner(self, args):
    if (args.find(' |') > (-1)):
        pname = self.program_name
        if ('.py' in pname):
            pname = ('python ' + pname)
        self.do_shell(('%s %s' % (pname, args)))
        return True
    return False
