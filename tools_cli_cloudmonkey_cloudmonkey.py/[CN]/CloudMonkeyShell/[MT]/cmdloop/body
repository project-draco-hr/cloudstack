def cmdloop(self, intro=None):
    print self.intro
    while True:
        try:
            super(CloudMonkeyShell, self).cmdloop(intro='')
            self.postloop()
        except KeyboardInterrupt:
            print '^C'
