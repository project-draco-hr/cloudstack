def complete_set(self, text, line, begidx, endidx):
    mline = line.partition(' ')[2]
    offs = (len(mline) - len(text))
    return [s[offs:] for s in ['host', 'port', 'apikey', 'secretkey', 'prompt', 'color', 'timeout', 'asyncblock', 'log_file', 'history_file'] if s.startswith(mline)]
