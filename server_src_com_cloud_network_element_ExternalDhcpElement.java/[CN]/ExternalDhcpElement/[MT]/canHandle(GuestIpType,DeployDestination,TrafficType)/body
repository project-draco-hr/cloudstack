{
  DataCenter dc=dest.getDataCenter();
  Pod pod=dest.getPod();
  if (pod.getExternalDhcp() && dc.getNetworkType() == NetworkType.Basic && trafficType == TrafficType.Guest && ipType == GuestIpType.Direct) {
    s_logger.debug("External DHCP can handle");
    return true;
  }
  return false;
}
