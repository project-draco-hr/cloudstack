def get_dns(self):
    dns = []
    dns.append(self.address().get_guest_ip())
    names = ['dns1', 'dns2']
    for name in names:
        if (name in self.cmdline().idata()):
            dns.append(self.cmdline().idata()[name])
    return dns
