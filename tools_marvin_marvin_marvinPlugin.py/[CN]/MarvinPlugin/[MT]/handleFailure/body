def handleFailure(self, test, err):
    '\n        Adds Failing test cases and information to log.\n        '
    err_msg = self.getErrorInfo(err)
    self.tcRunLogger.fatal(('%s: %s: %s' % (FAILED, self.testName, err_msg)))
    self.testResult = FAILED
