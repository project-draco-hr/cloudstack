def startTest(self, test):
    '\n        Currently used to record start time for tests\n        Dump Start Msg of TestCase to Log\n        '
    self.tcRunLogger.debug((('\n\n::::::::::::STARTED : TC: ' + str(self.testName)) + ' :::::::::::'))
    self.startTime = time.time()
