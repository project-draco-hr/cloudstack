{
  if (!(obj instanceof SuccessResponse) && !(obj instanceof ExceptionResponse))   sb.append("<" + obj.getResponseName() + ">");
  serializeResponseObjFieldsXML(sb,obj);
  if (!(obj instanceof SuccessResponse) && !(obj instanceof ExceptionResponse))   sb.append("</" + obj.getResponseName() + ">");
}
