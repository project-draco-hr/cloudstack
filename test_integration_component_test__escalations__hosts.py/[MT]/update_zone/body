def update_zone(self, state, zone_id):
    '\n    Function to Enable/Disable zone\n    '
    zone_status = self.zone.update(self.apiclient, id=zone_id, allocationstate=state)
    return zone_status.allocationstate
