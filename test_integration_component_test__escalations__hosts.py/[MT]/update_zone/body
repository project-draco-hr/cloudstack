def update_zone(apiclient, state, zone_id):
    '\n    Function to Enable/Disable zone\n    '
    zone_status = Zone.update(apiclient, id=zone_id, allocationstate=state)
    return zone_status.allocationstate
