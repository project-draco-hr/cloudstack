def update_host(self, state, host_id):
    '\n    Function to Enable/Disable Host\n    '
    host_status = Host.update(self.apiclient, id=host_id, allocationstate=state)
    return host_status.resourcestate
