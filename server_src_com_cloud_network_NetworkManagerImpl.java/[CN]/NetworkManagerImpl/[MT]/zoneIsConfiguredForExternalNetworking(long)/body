{
  DataCenterVO zone=_dcDao.findById(zoneId);
  return (zone.getGatewayProvider().equals(Network.Provider.ExternalFirewall.getName()) && zone.getFirewallProvider().equals(Network.Provider.ExternalFirewall.getName()) && zone.getLoadBalancerProvider().equals(Network.Provider.ExternalLoadBalancer.getName()));
}
