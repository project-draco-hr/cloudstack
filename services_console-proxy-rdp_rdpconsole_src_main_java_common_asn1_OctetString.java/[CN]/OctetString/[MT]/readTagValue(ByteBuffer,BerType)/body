{
  long length=buf.readBerLength();
  if (length > buf.length)   throw new RuntimeException("BER octet string is too long: " + length + " bytes. Data: "+ buf+ ".");
  value=buf.readBytes((int)length);
}
