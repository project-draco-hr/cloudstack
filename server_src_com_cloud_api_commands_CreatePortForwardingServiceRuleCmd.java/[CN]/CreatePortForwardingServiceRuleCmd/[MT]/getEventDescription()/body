{
  return "creating port forwarding rule on service: " + getPortForwardingServiceId() + ", public port: "+ getPublicPort()+ ", priv port: "+ getPrivatePort()+ ", protocol: "+ ((getProtocol() == null) ? "TCP" : getProtocol());
}
