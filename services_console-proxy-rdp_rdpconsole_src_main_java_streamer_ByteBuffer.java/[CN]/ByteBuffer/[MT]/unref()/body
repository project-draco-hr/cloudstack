{
  refCount--;
  if (refCount == 0) {
    if (parentByteBuffer != null) {
      parentByteBuffer.unref();
      parentByteBuffer=null;
    }
 else {
      BufferPool.recycleBuffer(data);
    }
    data=null;
  }
}
