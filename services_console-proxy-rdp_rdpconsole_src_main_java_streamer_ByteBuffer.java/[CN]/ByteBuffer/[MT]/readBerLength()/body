{
  int firstByte=readUnsignedByte();
  long result;
  if ((firstByte & 0x80) == 0) {
    result=firstByte & 0x7f;
  }
 else {
    int intLength=firstByte & 0x7f;
    if (intLength != 0)     result=readUnsignedVarInt(intLength);
 else     return -1;
  }
  return result;
}
