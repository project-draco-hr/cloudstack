{
synchronized (this) {
    if (_reentranceCount > 0) {
      assert(_gateOwner == Thread.currentThread());
      onGateOpen();
      _reentranceCount--;
      assert(_reentranceCount == 0);
      _gateOwner=null;
      notifyAll();
    }
  }
}
