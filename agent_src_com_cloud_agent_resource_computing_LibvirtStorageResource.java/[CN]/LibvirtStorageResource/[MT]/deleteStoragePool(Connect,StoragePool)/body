{
  if (pool != null) {
    String uuid=pool.getUUIDString();
synchronized (getStoragePool(uuid)) {
      pool.destroy();
      pool.undefine();
      pool.free();
    }
    rmStoragePool(uuid);
  }
  return true;
}
