def assert_on_sg_flag(self, flag, listzones):
    self.assertEqual(listzones[0].securitygroupsenabled, flag, ('Security Group enabled flag is %s with created Zone' % listzones[0].securitygroupsenabled))
    return
