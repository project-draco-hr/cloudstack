@attr(tags=['advanced', 'basic', 'vgpu'], required_hardware='true')
def destroy_vm(self):
    'Test destroy Virtual Machine\n        '
    self.virtual_machine.delete(self.apiclient)
    self.virtual_machine.getState(self.apiclient, 'Destroyed')
    return
