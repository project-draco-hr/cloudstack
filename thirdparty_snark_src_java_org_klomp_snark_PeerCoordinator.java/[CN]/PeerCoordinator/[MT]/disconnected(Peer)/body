{
  log.log(Level.FINER,"Disconnected " + peer);
synchronized (peers) {
    if (peers.remove(peer)) {
      unchokePeer();
    }
  }
  if (listener != null) {
    listener.peerChange(this,peer);
  }
}
