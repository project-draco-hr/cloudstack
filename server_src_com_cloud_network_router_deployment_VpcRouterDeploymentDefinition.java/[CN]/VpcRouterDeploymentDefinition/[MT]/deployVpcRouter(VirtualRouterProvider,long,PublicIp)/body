{
  LinkedHashMap<Network,List<? extends NicProfile>> networks=createVpcRouterNetworks(new Pair<Boolean,PublicIp>(true,sourceNatIp),this.vpc.getId());
  DomainRouterVO router=nwHelper.deployRouter(this,vrProvider,svcOffId,networks,true,vpcMgr.getSupportedVpcHypervisors());
  return router;
}
