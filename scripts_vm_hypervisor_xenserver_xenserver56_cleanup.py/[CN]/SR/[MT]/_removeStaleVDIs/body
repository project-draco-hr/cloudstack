def _removeStaleVDIs(self, uuidsPresent):
    for uuid in self.vdis.keys():
        if (not (uuid in uuidsPresent)):
            Util.log(('VDI %s disappeared since last scan' % self.vdis[uuid].toString()))
            del self.vdis[uuid]
