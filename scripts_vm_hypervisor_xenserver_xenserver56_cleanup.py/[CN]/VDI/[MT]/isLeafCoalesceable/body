def isLeafCoalesceable(self):
    'A VDI is leaf-coalesceable if it has no siblings and is a leaf'
    return ((not self.scanError) and self.parent and (len(self.parent.children) == 1) and (not self.hidden) and (len(self.children) == 0))
