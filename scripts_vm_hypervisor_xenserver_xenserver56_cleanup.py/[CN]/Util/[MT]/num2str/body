def num2str(number):
    for prefix in ('G', 'M', 'K'):
        if (number >= Util.PREFIX[prefix]):
            return ('%.2f%s' % ((float(number) / Util.PREFIX[prefix]), prefix))
    return ('%s' % number)
