{
  ManagedObjectReference morNetwork=getNetworkMor(portGroupName);
  if (morNetwork != null)   return (List<ManagedObjectReference>)_context.getVimClient().getDynamicProperty(morNetwork,"vm");
  return null;
}
