{
  Long userId=UserContext.current().getUserId();
  String name=cmd.getCfgName();
  String value=cmd.getValue();
  updateConfiguration(userId,name,value);
  if (_configDao.getValue(name).equalsIgnoreCase(value))   return true;
 else   return false;
}
