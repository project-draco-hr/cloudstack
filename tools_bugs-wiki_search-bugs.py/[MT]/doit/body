def doit(term):
    if (not term):
        return
    issues = soap.getIssuesFromTextSearch(auth, term)
    if (len(issues) == 0):
        return
    for issue in issues:
        print ('Found %s in %s' % (term, issue['key']))
