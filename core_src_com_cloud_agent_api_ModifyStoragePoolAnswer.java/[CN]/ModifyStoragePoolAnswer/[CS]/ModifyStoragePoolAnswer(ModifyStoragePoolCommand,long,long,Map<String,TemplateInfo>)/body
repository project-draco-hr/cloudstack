{
  super(cmd);
  this.result=true;
  this.poolInfo=new StoragePoolInfo(null,cmd.getPool().getName(),cmd.getPool().getHostAddress(),cmd.getPool().getPath(),cmd.getLocalPath(),cmd.getPool().getPoolType(),capacityBytes,availableBytes);
  this.templateInfo=tInfo;
}
