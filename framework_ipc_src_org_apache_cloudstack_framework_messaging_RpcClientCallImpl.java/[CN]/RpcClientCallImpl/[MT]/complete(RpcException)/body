{
  _responseResult=e;
synchronized (_responseLock) {
    _responseDone=true;
    _responseLock.notifyAll();
  }
  for (  RpcCallbackListener listener : _callbackListeners)   listener.onFailure(e);
}
