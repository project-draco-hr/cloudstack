def get_ips(self):
    ret = []
    for dev in self.dbag:
        if (dev == 'id'):
            continue
        for ip in self.dbag[dev]:
            ret.append(CsInterface(ip, self.config))
    return ret
