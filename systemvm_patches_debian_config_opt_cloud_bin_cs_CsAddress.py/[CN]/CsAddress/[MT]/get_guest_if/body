def get_guest_if(self):
    '\n        Return CsInterface object for the lowest guest interface\n        '
    ipr = []
    for ip in self.get_ips():
        if ip.is_guest():
            ipr.append(ip)
    if (len(ipr) > 0):
        return sorted(ipr)[(-1)]
    return None
