{
  List<String> rules=new ArrayList<String>();
  rules.add("You may think you're the greatest developer in the " + "world but every change to the API must be reviewed and approved. ");
  rules.add("Every API must have unit tests written against it.  And not it's unit tests");
  rules.add("");
  return rules;
}
