{
  try {
    Xen vms=new Xen(c);
    return vms.getRunningVmConfigs();
  }
 catch (  Exception e) {
    s_logger.debug("getting VM list from " + _host + " failed",e);
    throw new CloudRuntimeException("Exception on getting VMs from " + _host + ":"+ e.getMessage(),e);
  }
}
