{
  try {
    cleanupNetwork(vm.getVmVifs());
  }
 catch (  XmlRpcException e) {
    s_logger.debug("Clean up network for " + vm.getVmName() + " failed",e);
  }
  String vmName=vm.getVmName();
  this._vmStats.remove(vmName);
}
