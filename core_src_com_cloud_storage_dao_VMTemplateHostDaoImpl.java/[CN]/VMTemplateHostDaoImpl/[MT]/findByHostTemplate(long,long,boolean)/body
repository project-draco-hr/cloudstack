{
  SearchCriteria<VMTemplateHostVO> sc=HostTemplateSearch.create();
  sc.setParameters("host_id",hostId);
  sc.setParameters("template_id",templateId);
  if (!lock)   return findOneIncludingRemovedBy(sc);
 else   return lock(sc,true);
}
