{
  ImageStoreImpl imgStore=(ImageStoreImpl)store;
  Map<String,String> details=_imageStoreDetailsDao.getDetails(imgStore.getId());
  return new S3TO(imgStore.getId(),imgStore.getUuid(),details.get(ApiConstants.S3_ACCESS_KEY),details.get(ApiConstants.S3_SECRET_KEY),details.get(ApiConstants.S3_END_POINT),details.get(ApiConstants.S3_BUCKET_NAME),Boolean.parseBoolean(details.get(ApiConstants.S3_HTTPS_FLAG)),Integer.valueOf(details.get(ApiConstants.S3_CONNECTION_TIMEOUT)),Integer.valueOf(details.get(ApiConstants.S3_MAX_ERROR_RETRY)),Integer.valueOf(details.get(ApiConstants.S3_SOCKET_TIMEOUT)),imgStore.getCreated());
}
