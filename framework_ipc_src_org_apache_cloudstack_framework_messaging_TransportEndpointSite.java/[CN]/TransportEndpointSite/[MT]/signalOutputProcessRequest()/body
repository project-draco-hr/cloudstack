{
  boolean proceed=false;
synchronized (this) {
    if (_outstandingSignalRequests == 0) {
      _outstandingSignalRequests++;
      proceed=true;
    }
  }
  if (proceed)   _provider.requestSiteOutput(this);
}
