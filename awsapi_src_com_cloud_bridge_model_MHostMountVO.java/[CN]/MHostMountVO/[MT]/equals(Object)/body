{
  if (this == other)   return true;
  if (!(other instanceof MHostMountVO))   return false;
  return getMhost().equals(((MHostMountVO)other).getMhost()) && getShost().equals(((MHostMountVO)other).getShost());
}
